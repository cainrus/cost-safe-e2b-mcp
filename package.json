{
  "name": "cost-safe-e2b-mcp",
  "version": "1.0.0",
  "type": "module",
  "license": "Apache-2.0",
  "description": "MCP server that runs code in an E2B sandbox and kills it after every call. Drop-in replacement for the archived @e2b/mcp-server, which creates a sandbox per call and never kills it.",
  "bin": { "cost-safe-e2b-mcp": "server.mjs" },
  "files": ["server.mjs", "LICENSE", "NOTICE", "README.md"],
  "keywords": ["mcp", "model-context-protocol", "e2b", "sandbox", "code-interpreter"],
  "author": "Sergei Tarassov <cainrus@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cainrus/cost-safe-e2b-mcp.git"
  },
  "dependencies": {
    "@e2b/code-interpreter": "^1.5.1",
    "@modelcontextprotocol/sdk": "^1.24.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  }
}
